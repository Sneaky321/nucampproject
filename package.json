{
  "name": "nucampproject",
  "version": "1.0.0",
  "description": "A project using Bootstrap",
  "main": "index.js",
  "scripts": {
    "start": "lite-server",
    "build": "echo 'No build script defined' && exit 0",
    "test": "echo 'No test script defined' && exit 0"
  },
  "keywords": [
    "bootstrap",
    "web",
    "frontend"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.3.0"
  },
  "devDependencies": {
    "lite-server": "^2.6.1"
  },
  "browser-sync": {
    "files": [
      "./**/*.html",
      "./**/*.css",
      "./**/*.js"
    ],
    "server": {
      "baseDir": "./"
    }
  }
}
